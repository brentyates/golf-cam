# Private configuration template
# Copy this file to .env.local and fill in your specific values
# .env.local is gitignored and will not be committed

# Your Raspberry Pi's IP address (local network or public IP)
PI_IP_ADDRESS=raspberrypi.local

# SSH username (default: pi)
PI_USER=pi

# Remote path on Pi (default: /home/pi/swing-cam)
PI_REMOTE_PATH=/home/pi/swing-cam
